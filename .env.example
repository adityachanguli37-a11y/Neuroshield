MONGODB_URI=mongodb+srv://user:password@cluster0.example.mongodb.net/neuroshield?retryWrites=true&w=majority
JWT_SECRET=replace_with_a_unique_32_plus_character_secret
SESSION_COOKIE_NAME=neuroshield_session
# Required only by `npm run seed`; use a temporary value and rotate it afterwards.
DEMO_SEED_PASSWORD=
PORT=5000
NODE_ENV=development
